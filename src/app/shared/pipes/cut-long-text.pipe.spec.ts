import { CutLongTextPipe } from './cut-long-text.pipe';

describe('CutLongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CutLongTextPipe();
    expect(pipe).toBeTruthy();
  });
});
